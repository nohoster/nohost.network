apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-values
  namespace: monitoring
data:
  values.yaml: |
    server:
      persistentVolume:
        enabled: false
      retention: "1d"
    
    alertmanager:
      enabled: false
    
    prometheus-node-exporter:
      enabled: false

